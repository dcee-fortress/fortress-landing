import ProjectToDateReport from "@/components/project/ProjectToDateReport"

export default function ProjectToDateView({ projectName, dashboard }) {
  return <ProjectToDateReport projectName={projectName} dashboard={dashboard} />
}
